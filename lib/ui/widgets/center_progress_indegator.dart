import 'package:flutter/material.dart';
class Center_progress_indegator extends StatelessWidget {
  const Center_progress_indegator({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}